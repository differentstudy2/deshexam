import React from 'react';

export const metadata = {
    title: 'Question Bank | DeshExam',
    description: 'Explore the complete Question Bank on DeshExam. Find thousands of questions, answers, and explanations.',
};

export default function QuestionsLayout({ children }: { children: React.ReactNode }) {
    return <>{children}</>;
}
